import React from 'react';
import logo_src from '../assets/images/logo.svg'

const Logo = () => {
    return (
        <div>
            <img src={logo_src}/>
        </div>
    );
};

export default Logo;
